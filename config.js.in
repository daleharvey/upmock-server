var config = {};

config.couch = {
  admin: {
    name: %COUCH_USER%,
    pass: %COUCH_PASSWORD%
  },
  host: %COUCH_HOST%,
  port: %COUCH_PORT%
};

config.node = {
  host: %UPMOCK_HOST%,
  port: %UPMOCK_PORT%
};

config.googleApiKey = %GOOGLE_API_KEY%;

exports.config = config;